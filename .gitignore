bin/
obj/
.vs/
node_modules/
dist/
publish/
publish.zip
*.zip

# Installer build output — payload is a ~215 MB self-contained publish,
# regenerated by installer/build.ps1. Only the scripts belong in git.
installer/payload/
installer/output/
# 266 MB SQL Express redistributable, re-downloaded by build.ps1 when absent
installer/prereq/
